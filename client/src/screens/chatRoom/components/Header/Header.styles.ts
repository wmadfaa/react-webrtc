import {makeStyles} from '@material-ui/core/styles';

export default makeStyles({
  root: {
    position: 'absolute',
    color: 'white',
    fontWeight: 600,
    fontSize: 16,
    whiteSpace: 'nowrap',
    top: 20,
    left: 20,
    float: 'left',
  },
  logo: {
    float: 'left',
    padding: 7,
  },
});
